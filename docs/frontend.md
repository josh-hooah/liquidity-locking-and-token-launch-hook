# Frontend

Static launch console in `frontend/`.

Features:
- launch and policy setup
- pool initialization + liquidity seeding calls
- swap sequence execution
- permissionless `advance`
- unlock progress and withdraw flow

Run from repo root:

```bash
python3 -m http.server 8080
```

Open: `http://localhost:8080/frontend/`
